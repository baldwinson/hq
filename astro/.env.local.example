# Local development with Docker Compose
# Copy this to .env and run `docker compose up -d` from the project root

# PostgreSQL connection (matches docker-compose.yml defaults)
DATABASE_URL=postgresql://baldwinson:baldwinson_dev@localhost:5433/baldwinson
